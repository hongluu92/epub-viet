'use client';

export default function SearchPage() {
  return (
    <div className="min-h-screen" style={{ backgroundColor: 'var(--bg)' }}>
      <p style={{ color: 'var(--text)', padding: '2rem' }}>Search placeholder</p>
    </div>
  );
}
